 ```markdown

① 两个模型的核心差异体现在什么机制上？  
**答案：B. 是否为超时或低位作为生成条件**  
**解析：** 无条件生成不依赖外部条件，而条件生成通过学号尾数（低位）作为国家信息的条件，因此核心差异在于是否使用条件输入。

---

② 在条件生成模型（Model_Conditioned_Surname_Generation）中，国家信息通过什么方式影响生成过程？  
**答案：B. 作为 GRU 的初始函数状态**  
**解析：** 国家信息通过嵌入层（`nation_emb`）转换为向量，并作为 RNN 的初始隐藏状态来引导生成过程。

---

③ 文件 2 中新增的 `nation_emb` 层的主要作用是：  
**答案：B. 将目标标签转换为隐藏状态初始化向量**  
**解析：** `nn.Embedding` 层将国家标签（整数）映射为与 RNN 隐藏层同维度的向量，用于初始化 RNN 的隐藏状态。

---

④ 对比两个文件的 `sample_from_model` 函数，文件 2 新增了哪个关键参数？  
**答案：B. nationalities**  
**解析：** 条件生成需要传入国家信息（`nationalities`）作为生成条件，因此该参数是新增的关键参数。
```